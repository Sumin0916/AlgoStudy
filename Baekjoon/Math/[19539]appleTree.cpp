#include<bits/stdc++.h>

using namespace std;

int N;
int T[100001];

int main(void) {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    cin >> N;
    for(int i = 0; i < N; i++){cin >> T[i];}
    
    return 0;
}