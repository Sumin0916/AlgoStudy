#include <iostream>

using namespace std;
int parent[100001] = {0, };

int find_parent(int a, int b){

}

int main(void){
    int n, m, k;
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin>>n>>m>>k;

    return 0;
}