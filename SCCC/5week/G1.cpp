#include<iostream>
#include<vector>

using namespace std;
int t;

void solve() {
    int n;
    vector<int> a(5001);
    cin >> n;
    for (int j = 0; j < n; j++) {cin >> a[j];}
    sort(a.begin(), a.end());

    int s = 1;

}

int main(void){
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin >> t;
    for (int i = 0; i < t; i++) {
    }
    return 0;
}